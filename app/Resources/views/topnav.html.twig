<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">

            {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                {% if app.user.username == "Tarek Brahim" %}
                    <li>
                        <a class="navbar-brand" href="/">AIL Administration</a>
                    </li>
                {% else %}
                    <li>
                        <a class="navbar-brand" href="/">AIL</a>
                    </li>
                {% endif %}
            {% else %}
                <li>
                    <a class="navbar-brand" href="/">AIL</a>
                </li>
            {% endif %}
        </div>
        <div id="navbar" class="collapse navbar-collapse">
            <ul class="nav navbar-nav navbar-right">
                {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                    {% if app.user.username == "Tarek Brahim" %}
                        <li>
                            <a href="/backend/create">Add</a>
                        </li>
                    {% else %}
                        <li>
                            <a href="/profile">
                                {{ 'layout.logged_in_as'|trans({'%username%': app.user.username},
                'FOSUserBundle') }}
                            </a>
                        </li>
                        <li>
                            <a href="/profile/edit">Edit profile</a>
                        </li>
                    {% endif %}
                    <li>
                        <a href="/common/directions">Directions</a>
                    </li>
                    <li>
                        <a href="/frontend/directions">Institutions</a>
                    </li>
                    <li>
                        <a href="{{ path('fos_user_security_logout') }}">
                            {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
                        </a>
                    </li>
                {% else %}
                    <li>
                        <a href="/register">Sign-up</a>
                    </li>
                    <li>
                        <a href="{{ path('fos_user_security_login') }}">
                            {{ 'layout.login'|trans({}, 'FOSUserBundle') }}</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
